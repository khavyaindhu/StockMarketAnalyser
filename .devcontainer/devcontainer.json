{
  "name": "StockMarketAnalyser",

  "postStartCommand": "pip install -q -r requirements.txt && python -m streamlit run app.py --server.port 8501 --server.address 0.0.0.0",

  "portsAttributes": {
    "8501": {
      "label": "Streamlit App",
      "onAutoForward": "openBrowser",
      "visibility": "public"
    }
  },

  "forwardPorts": [8501]
}
